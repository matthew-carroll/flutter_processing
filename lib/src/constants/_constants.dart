import 'dart:math';

// ignore_for_file: non_constant_identifier_names
class SketchConstants {
  final TWO_PI = pi * 2;
  final PI = pi;
  final HALF_PI = pi / 2;
  final QUARTER_PI = pi / 4;
  final TAU = 6.2831855;
}
