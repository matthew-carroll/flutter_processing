import 'dart:math';

import '_core.dart';

mixin SketchConstants on SketchContext {
  final double HALF_PI = pi / 2;
  final double PI = pi;
  final double QUARTER_PI = pi / 4;
  final double TWO_PI = 2 * pi;
  final double TAU = 2 * pi;
}
