name: flutter_processing
description: A Flutter version of Processing.
version: 0.2.0
homepage: https://github.com/matthew-carroll/flutter_processing

environment:
  sdk: ">=2.17.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  bitmap_canvas:
    git:
      url: git@github.com:Flutter-Bounty-Hunters/bitmap_canvas.git
      ref: head
  fast_noise: ^1.0.1
  image: ^3.0.2
  path: ^1.8.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

  golden_toolkit: ^0.9.0

flutter:
